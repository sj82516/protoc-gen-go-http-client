syntax = "proto3";

option go_package = "protoc-gen-go-http-client/example";

message Hello {
  option (message_options).ignore = true;
  string text = 1;
}

message User {
  int64 id = 1;
}

service HelloService {
  rpc Get(User) returns (Hello);
}